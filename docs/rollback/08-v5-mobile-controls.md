# Rollback Note 08: V5 Mobile Controls

The mobile V5 controls hide the generic filter stack and keep the map controls visible. Revert the V5 media-query overrides to restore full filters above the map.
